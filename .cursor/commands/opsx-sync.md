---
name: /opsx-sync
id: opsx-sync
category: Workflow
description: Sync delta specs from a change to main specs
---

## Alias of **`/skillgrid-finish`** (section 1 — optional sync)

Open and follow **section 1** of **`skillgrid-finish.md`** (“Optional: sync delta specs to main”). Perform delta → main merges exactly as described there—**without** invoking separate skill files.

## Completion report (required)

State what you did (delta spec paths merged, main specs touched), token usage if the product shows it (else **`Token usage: not shown in this environment`**), and suggested next (**`/skillgrid-finish`** for archive/PR, or done if only sync was needed).
