---
description: Prefer strict TDD when the project supports tests — hub strict-TDD flag
alwaysApply: true
---

# Strict TDD mode

**Strict TDD: enabled** (project-dependent).

Behavioral checklist: `.agents/skills/skillgrid-tdd/SKILL.md` and **`sdd-apply`** skill. Respect slice labels and verifier gates — see `.agents/rules/skillgrid-sdd-enforcement.mdc`.
